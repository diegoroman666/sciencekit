"""SciencKit analysis engine — pure standard library.

The same modules run under CPython (Flask dev server) and under Pyodide
(the static build), so there is one implementation of the statistics.
"""

__version__ = "1.0.0"
